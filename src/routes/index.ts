// Export the main router component
export { default } from './AppRouter';

// Export route protection component
export { default as PrivateRoute } from './PrivateRoute';

// Export route constants and metadata
export { ROUTES, ROUTE_METADATA } from './constants';
export type { RouteKey } from './constants';
